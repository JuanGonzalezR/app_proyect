import 'package:flutter/material.dart';

class ProfileService extends StatelessWidget {
  const ProfileService({Key? key}) : super(key: key);

 @override
 Widget build(BuildContext context) {
  return const Center(
    child: Text('Profile Service'),
 );
 }
}